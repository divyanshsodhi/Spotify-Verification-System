<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Sign Up</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>

<body class="h-screen w-screen box-border bg-gray-900 flex flex-col items-center text-white">
    <div class="flex flex-col items-center gap-5 w-full sm:w-3/4 md:w-2/5 mt-10 bg-gray-800 p-8 rounded-lg">
        <!-- Page Title -->
        <h1 class="text-3xl font-bold mb-5">Enter your details for creating a new account</h1>

        <!-- Form Section -->
        <form action="/create" method="post" class="flex flex-col w-full gap-5">
            <!-- Email Input -->
            <div>
                <label for="email" class="block mb-2 text-sm font-medium">Email Address</label>
                <input type="email" id="email" name="email"
                    class="w-full p-3 rounded-md bg-gray-700 text-gray-300 placeholder-gray-500 focus:outline-none focus:ring-2 focus:ring-green-500"
                    placeholder="Enter your email" required>
            </div>

            <!-- Phone Number Input -->
            <div>
                <label for="phone" class="block mb-2 text-sm font-medium">Phone Number</label>
                <input type="tel" id="phone" name="phone"
                    class="w-full p-3 rounded-md bg-gray-700 text-gray-300 placeholder-gray-500 focus:outline-none focus:ring-2 focus:ring-green-500"
                    placeholder="Enter your phone number" required>
            </div>

            <!-- Password Input -->
            <div>
                <label for="password" class="block mb-2 text-sm font-medium">Password</label>
                <input type="password" id="password" name="password"
                    class="w-full p-3 rounded-md bg-gray-700 text-gray-300 placeholder-gray-500 focus:outline-none focus:ring-2 focus:ring-green-500"
                    placeholder="Create a password" required>
            </div>

            <!-- Submit Button -->
            <button type="submit"
                class="w-full h-12 bg-green-500 rounded-full text-center font-bold hover:bg-green-600 transition">
             Sign Up
            </button>
        </form>

        <!-- Redirect to Login -->
        <p class="text-sm mt-4">Already have an account? 
            <a href="/login" class="text-green-500 hover:underline">Log In</a>
        </p>
    </div>
</body>

</html>
